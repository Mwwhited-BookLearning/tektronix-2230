It seems that the registers might be inverted outputs for captured values as the SELECT_C1_C2 when unchecked is causing the system to do the debug boot.  

if I uncheck the value it does not output the serial data.  

the `4305063` files are from a fresh run with "SELECT_C1_C2"  "checked"

the `5391063` files are from a fresh run with "SELECT_C1_C2"  "unchecked"