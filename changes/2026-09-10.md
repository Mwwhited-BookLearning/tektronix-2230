# 2026-09-10

## The 0x80000-0x97FFF mystery region is much bigger than thought

- Mined the newly-disassembled comm ROM for far calls into the main
  ROM's address range - none were new (the combined recursive-descent
  run already resolves any in-range far call automatically), but this
  surfaced that the comm ROM makes **150+ distinct far calls** into
  the previously-flagged `0x80000-0x97xxx` region, across dozens of
  separate segments spanning all the way to `0x97C95` (~96KB total).
- Confirmed via TekWiki that there is no third ROM chip for the 2230
  (just the two 27512 EPROMs on board A10, plus the comm option ROM),
  ruling out "this is a ROM we simply don't have a dump of."
- Found direct evidence this is real, tested RAM instead: the main ROM
  contains a classic non-destructive RAM-size-test pattern (read,
  write `0xAA55`, restore) targeting `es=0x8000` - the textbook way
  period boot code detects how much memory is actually installed.
- Went looking for how code gets loaded into that RAM for the comm ROM
  to call into it (checked 2 of 5 call sites of a generic memcpy-style
  utility function, `SUB_FBC09`); didn't find it there. Still open -
  documented as a real, possibly hard limit on what static analysis of
  these three ROM dumps alone can recover, since RAM contents loaded
  at runtime (however they get there) aren't present in any ROM dump.
- Wrote up in `MEMORY_MAP.md` (including the PlantUML diagram) and
  `disasm/NOTES.md`; updated `TODO.md` with the concrete next checks
  (remaining 3 memcpy call sites, other possible loading mechanisms).

## Correction: the comm ROM is NOT bank-switched

- User pushed back on the bank-switching assumption ("i dont know why
  someone would use bank switching if they have enough address space
  to not use bank switching") and asked whether the comm ROM itself
  might just be sitting in the "missing memory hole." Checked it
  directly: every far-call target landing in `0x80000-0x8FFFF` was
  tested against the comm ROM's own function-start signatures at
  `file_offset = phys - 0x80000` — **82 of 84 distinct targets land
  exactly on a known function start.** The main ROM's already-proven
  code also makes several direct far calls straight into that range.
- Corrected: `160-2998` is a plain fixed-address 64KB device at
  physical `0x80000-0x8FFFF`, same simple pattern as the two main-ROM
  halves - no bank-switching at all. This resolved most of the
  "0x80000-0x97FFF mystery region" from earlier: the lower half was
  just the comm ROM at the wrong (arbitrary bookkeeping) address this
  project had been using for it. The genuine remaining mystery is
  narrower: `0x90000` and up.
- Reworked the tooling to match: `160-2998` is now registered directly
  in `gen_disasm_x86.CHIPS` like the other two chips (no more per-page
  virtual addressing). `gen_disasm_2998.py` simplified to a thin
  heuristic-entry-point supplement on the same real address space.
  `gen_source.py` gained an `only_chips` parameter so the comm ROM's
  heuristic-assisted coverage doesn't leak confidence into the main
  ROM's proven-only `.asm` files. Regenerated and re-validated
  everything: main ROM 10,488 instructions (9,480 exact + 1,003 alt-
  encoding, 0 mismatches, comm ROM now contributing 99 proven
  instructions); comm ROM with the heuristic layer included, 20,446
  instructions (18,290 exact + 2,151 alt-encoding, 2 real mismatches
  likely decode drift into header text, 3 unconverted).
- Reaching further into the main ROM surfaced x87 FPU instructions and
  a few rare string-I/O/bounds-check instructions the converter didn't
  handle yet; conservatively excluded from conversion (safe raw-`db`
  fallback) rather than risk getting their NASM syntax wrong. Also
  found and fixed a genuine encoding-duplicate case (opcode `0x82`
  mirrors `0x80` for byte-sized group-1 ops).
- Full write-up in `disasm/NOTES.md` ("The comm ROM is NOT bank-
  switched", "The 0x90000+ region"), `MEMORY_MAP.md`, and `TODO.md`.

## Jump map started

- Created `JUMP_MAP.md` per the user's request: PlantUML activity
  diagrams of control-flow relationships, starting high-level (the
  real observed boot sequence from CPU reset through interrupt-vector
  setup into what looks like the main init dispatcher, plus the main
  ROM/comm ROM cross-call relationship) with room to add more detailed
  per-subsystem diagrams as specific routines get identified.

## The 0x90000-0x97FFF region is fully resolved (address-decode alias)

- User asked whether the `0x90000-0x97FFF` targets might make sense as
  a "shadow" of `0x80000-0x8FFFF`. A plain `+0x10000` shift didn't fit
  (1/82 matched), but brute-forcing every possible base offset against
  all 82 observed far-call targets found `base=0x88000` gives **82/82
  exact matches** against the comm ROM's own function-start signatures.
  So `0x90000-0x97FFF` is the same comm-ROM bytes as `0x88000-0x8FFFF`
  (file offset `0x8000-0xFFFF`, its own upper half/pages 2-3), visible
  a second time - almost certainly incomplete address-line decoding in
  the comm ROM's chip-select logic, not a separate device.
- Wired this into the tooling: `gen_disasm_x86.CHIPS` gained a
  `"2998_alias_90000"` entry (a `slice` of the same comm-ROM file,
  mapped at `0x90000`) and `load_chips()` learned to honor `slice`.
  This alone grew the main disassembly from 10,488 to **13,510**
  proven-reachable instructions (comm ROM's own proven-reachable count
  went from 99 to 397, now that its aliased calls resolve too).
- Re-validated everything (0 mismatches after also fixing `loopne`/
  `loope`/`loopz`/`loopnz`, which had the same missing-near-branch-
  handling bug `loop` alone used to have) and regenerated all three
  `.asm` files plus the comm ROM's heuristic-included pass.

## Found: the option-board presence/RAM-detection routine

- User asked two connected questions: might the `0xAA55` pattern
  (previously read as a generic RAM-size test) be an install-check for
  the comm option card, and might it involve RAM/memory-mapped I/O on
  the comm board specifically? **Both confirmed correct - it's one
  routine (`SUB_E44F1`, `160-3633`) doing both in sequence:**
  1. Checks `ES:DI = 0x8000:0x0004` (physical `0x80004`) for a valid
     ROM-header-style checksum (byte + one's-complement = `0xFF` - the
     exact same pattern used in every ROM's own self-ID header,
     decoded on day one of this project).
  2. Only if that passes, tests whether the same address is *also*
     writable (the `0xAA55` save/write/verify/restore sequence),
     gated additionally on a config byte at `[0x1B83] == 0x1E`.
  3. Packs the result into status byte `[0x1BF9]`.
- This ties directly back to the `"Function requires options not
  installed in this instrument"` string found in `160-3532` at the
  very start of the project.
- Found this function's caller is `SUB_E416F` (`160-3633`), which
  turned out to be a **self-test dispatcher**: ~25 calls to individual
  subsystem-test subroutines in sequence, each folding a return code
  into an accumulator. `SUB_E44F1`'s call is notably NOT folded into
  that accumulator the same way its neighbors are - consistent with
  "is an option installed" being informational rather than pass/fail.
  Full call list and inferred variable roles (`[0x1B10]`, `[0x1B18]`,
  `[0x1BF9]`, `[0x1B83]`) written up in `disasm/NOTES.md`.
- Added activity diagrams for both to `JUMP_MAP.md`.

## Function list and maintenance reminders

- Created `FUNCTIONS.md`: a human-readable index of every identified
  routine/variable across all three ROMs (address, label, purpose,
  confidence), companion to the `.symbols.json` files. Seeded with
  everything found this session (`SUB_E416F`, `SUB_E44F1`, `SUB_E6AAB`,
  `SUB_FBC09`, and the 4 tracked variables).
- Updated `CLAUDE.md` with explicit reminders to keep `MEMORY_MAP.md`,
  `JUMP_MAP.md`, and `FUNCTIONS.md` current (not just append to them),
  and to record generally-useful techniques (like the brute-force-
  every-base-offset trick that resolved both the comm ROM's real
  address and its alias) in `disasm/NOTES.md` for reuse.

## Variables list, strings/constants catalog, and language assessment

- Split the small variable table out of `FUNCTIONS.md` into its own
  `VARIABLES.md`, per the user's request.
- User asked whether this firmware looks hand-written or compiled, and
  if compiled, what language. Answer: **compiled C**, not hand-written
  assembly. Strongest evidence: the HPGL plotter code uses literal
  `%c`/`%d` printf-style format strings (e.g. `"PU%d,%d;"`) - C's
  standard-library convention specifically, not something Pascal does.
  Supporting evidence: uniform compiler-template prologues/epilogues
  on every function, repeated near-identical function pairs, `les`/
  `lds` far-pointer idioms, and null-terminated string literals
  throughout the main ROM (147/169 and 149/171 of all strings found).
  One piece of evidence initially looked like it argued for Pascal -
  the comm ROM's GPIB command table uses length-prefixed strings
  (131/185), and the pervasive `retf N` callee-cleanup calling
  convention is literally called "the Pascal calling convention" - but
  both are better explained as a hand-built fast-parsing table format
  and a deliberate embedded-C code-size optimization, respectively,
  given the much harder-to-fake printf-format-string evidence. Full
  write-up in `disasm/NOTES.md` "What language was this written in?".
- Extracted every printable string from all three ROMs directly from
  the raw binaries (independent of disassembly) and classified each as
  length-prefixed or null-terminated, confirming the split cleanly by
  ROM/table. Surfaced two big finds along the way: what looks like the
  actual front-panel menu/parameter tree names (`160-3633`, e.g.
  `ACQ_MODE_SETUP_TABLE`), and the self-test dispatcher's printed
  diagnostic output text (`160-3532`, e.g. `"2230/2220 Power up tests
  complete."`, `"ROM/RAM/NMI :"`) - directly confirming and enriching
  the self-test dispatcher found earlier this session.
- Wrote up as `STRINGS.md` (curated, categorized, human-readable) with
  a machine-readable companion `disasm/strings_<rom>.json` per ROM,
  generated by the new `disasm/gen_strings.py` (regenerate if a ROM
  binary ever changes).

## Applied renames; built the mechanism to make them stick

- User asked to use everything found so far to give functions/jumps/
  variables better semantic names. Renamed the four routines with
  strong-enough evidence: `0xE00B1` → `boot_init`, `0xE416F` →
  `self_test_dispatcher`, `0xE44F1` → `check_comm_option_installed`,
  `0xFBC09` → `memcpy_far`.
- Discovered the renaming mechanism documented in `CLAUDE.md`
  ("rename in `.symbols.json`, the generator re-renders from the
  symbol table") wasn't actually implemented — `render()` always
  recomputed the `SUB_XXXXX`/`L_XXXXX` placeholder from scratch,
  ignoring any prior `functional_name`. Fixed by adding a
  `FUNCTIONAL_NAMES` dict directly in `gen_disasm_x86.py` (address →
  name, with the evidence as a comment) that `render()`,
  `gen_source.py`, and `gen_disasm_2998.py` all now check first -
  renames are code, versioned and auditable, and actually appear in
  the regenerated `.lst`/`.asm`/`.symbols.json` (both `name` and
  `functional_name`) rather than needing a fragile "preserve prior
  edits across regeneration" step. Updated `CLAUDE.md`'s convention
  note to describe the real mechanism.
- Tried to identify the self-test dispatcher's ~20 remaining
  subsystem-test subroutines by searching for direct references to
  the diagnostic message strings found earlier (`HS_ACQ`, `COMM_ROM`,
  etc.) from within each subroutine's decoded body - found zero direct
  references, despite each having substantial real code (100-200+
  instructions). Followed the dispatcher's caller one step further and
  found `SUB_E094B`, called immediately after the dispatcher returns,
  which logs a small record into a buffer rather than printing -
  likely why. Documented as a genuine open thread rather than guessing
  names from string proximity/call order without that missing link.
  Regenerated and re-validated everything (0 mismatches, same 2 known
  decode-drift cases as before).

## Clean pseudocode reconstructions, and a real addressing correction

- User asked for "clean code to understand" as the next step. Added
  `PSEUDOCODE.md`: hand-written C reconstructions of the four renamed
  routines. `memcpy_far` and `check_comm_option_installed` are fully
  understood and should be trustworthy line-for-line; `boot_init` and
  `self_test_dispatcher` are long and only partially analyzed, so
  their reconstructions keep the accurate structure but call out each
  open question as a comment rather than guessing.
- Writing `check_comm_option_installed`'s pseudocode surfaced a real
  mistake in earlier documentation: the RAM/IO write-test was recorded
  as targeting "the same address" as the ROM-header checksum check
  (physical `0x80004`). Re-deriving it from the actual instruction
  (`DI` unchanged at `4`, displacement `0xA000`) shows it actually
  targets a *different* word, physical `0x8A004`. Corrected in
  `disasm/NOTES.md`, `FUNCTIONS.md`, and `JUMP_MAP.md`. Worth
  remembering: always re-derive an effective address from the actual
  bytes rather than trusting an earlier paraphrase, even one written
  earlier in the same investigation.

## Main ROM heuristic layer: coverage jumps to 85.6% combined

- Continued disassembling per the user's request. First tried
  widening proven coverage: broadened the interrupt-vector-write
  scanner to registers other than `bx`, and re-checked for unresolved
  indirect jmp/call and unmapped far-call targets across the now much
  larger reachable set. All three came back empty/false-positive -
  the broadened IVT scan's hits turned out to be a shared utility
  storing a computed far pointer through ordinary scratch variables
  that happen to sit in the IVT's address range, not real vectors.
  Confirmed: 0 indirect jmp/call anywhere, 0 unmapped far-call targets
  anywhere. Growing *proven* coverage further needs a genuinely new
  kind of entry point, not more of the same techniques - that lever is
  exhausted for now.
- Instead applied the comm ROM's successful heuristic technique (scan
  for the `55 8B EC` push-bp/mov-bp,sp prologue) to the main ROM pair:
  found 216 unreached signatures in `160-3633` and 209 in `160-3532`.
  New `disasm/gen_disasm_mainrom_heuristic.py` seeds them, output to a
  separate `sysrom_3532_3633_heuristic.lst` (the official
  `sysrom_3532_3633.lst` stays proven-only, same confidence-separation
  principle as the comm ROM). Result: both main-ROM halves jumped from
  ~14-41% to **90.6% coverage each**.
- New `disasm/validate_all.py` NASM-validates everything (all four
  mapped regions, all heuristic layers) in one combined pass: **68,776
  instructions total, 85.6% of all mapped bytes** (196,372/229,376) -
  61,771 exact + 6,790 alt-encoding + 174 not-converted + only 41 real
  mismatches.
- Chasing those 41 mismatches down surfaced and fixed several more
  real `validate_nasm.py` bugs: `jcxz` doesn't accept the `short`
  qualifier the way other `jcc` mnemonics do (was crashing the whole
  batch for any file containing one); `cmpsb`/`cmpsw` have their
  source/dest operand order reversed from `movsb`/`movsw` in
  capstone's output; a whole class of alternate-encoding helpers
  didn't account for a leading segment-override prefix byte, so they
  misread the prefix itself as the opcode (added a shared
  `split_seg_prefix()`); a new alternate-encoding case (`mod=01`/
  `mod=10` with a zero displacement can also be encoded as `mod=00`);
  and a stray `0xF2`/`0xF3` prefix in front of a non-string
  instruction (legal on real hardware, no NASM syntax for it).
- The remaining ~39 mismatches cluster in one narrow region of
  `160-3633` (physical `0xEA1A0-0xEA615`) decoding as 386-only
  features (`fs`/`gs` prefixes, `popal`/`popaw`, `esi`) that can't be
  real on this confirmed 8086/8088 - genuine decode drift into
  non-code bytes. Traced: these addresses have no label at all (not a
  jump/call target, not a heuristic entry) - reached by pure
  sequential fallthrough across an undetected code/data boundary. Left
  as documented, not patched around - the validator's exact-match-only
  trust model already keeps it from affecting the buildable `.asm`.
- Re-verified the official proven-only files are unaffected (still 0
  mismatches) and regenerated all buildable `.asm` sources.

## NOP-aligned "readable" reconstruction, saved as a permanent artifact

- Tested and confirmed NASM's `CPU` directive does not resolve any of
  the encoding-choice ambiguities (direction bit, immediate/
  displacement width, zero-displacement collapse, duplicate opcode) -
  it only gates instruction-set availability, not encoding choice
  among legal alternatives for a given CPU level.
- Built `disasm/gen_source_readable.py`: emits every decoded
  instruction as real NASM text (no raw `db` fallback for anything
  actually understood), padding any NASM-preferred-shorter encoding
  back out to the original instruction length with explicit `nop`
  bytes so every following address is unchanged from the true
  original.
- Verified via independent reassembly: all three chips reassemble to
  their true original length exactly, with only 1,431 (`3633`) / 324
  (`3532`) / 108 (`2998`) bytes differing out of 65,536, all of them
  NASM's own equivalent encoding substituted for the original's.
- Per direct instruction, saved the assembled result permanently as
  `binary/aligned/160-XXXX-14_aligned.bin`, clearly marked **not
  original firmware** (`binary/aligned/README.md`), and adopted it as
  the reference binary for future checks going forward - flagged in
  `TODO.md`/`disasm/NOTES.md` to revisit that choice once the rest of
  the analysis is further along.

## Correction: self_test_dispatcher was misnamed; found the readout/CRT display memory

- Tracing `SUB_E094B` (called right after the old
  `self_test_dispatcher`) led to reading `0xE416F` end-to-end for the
  first time, instead of just skimming its call list as before.
  Found it contains **zero** OR-fold test calls - it's entirely
  banner-printing. Renamed it `print_selftest_banner`. The real
  dispatcher (the ~14-call OR-fold pattern previously described here)
  is a different function, `0xE4244`, called from an unrelated site
  (`0xE3DEE`) - it now carries the `self_test_dispatcher` name.
- Three routines previously counted among the dispatcher's
  unidentified "subsystem-test subroutines" (`SUB_E374E`, `SUB_E3821`,
  `SUB_E0AF5`) turned out to be display/print primitives, not tests -
  found by tracing the literal far-pointer arguments they're called
  with back to a fixed string-table segment (`0xFF7B`, landing in
  `160-3532`). Renamed the chain: `print_string_far` (`0xE0AF5`) →
  `print_char` (`0xE0B2A`) → `write_readout_port_byte` (`0xE0B50`).
- `write_readout_port_byte` writes its byte argument to a **fixed**
  physical address, `0x40000+0x6F0`, never incremented - strong
  evidence of a hardware port (character generator with its own
  internal cursor) rather than plain RAM. This is very likely the
  scope's front-panel/CRT **readout** character-generator write port.
  Found two more candidate single-byte read ports nearby,
  `0x41000`/`0x42000` (`SUB_E4429`/`SUB_E440A`, not yet renamed -
  purpose unconfirmed).
- Also found (via `append_readout_char`, `0xE39F0`, and `SUB_E0BA3`) a
  second "plane" of a related line buffer, `0x8000` higher in the
  same `0x4000` segment (physical `0x48000-0x4FFFF`) - candidate
  attribute/shadow plane or per-channel diagnostic scratch, not yet
  confirmed which.
- Updated `MEMORY_MAP.md` (new I/O region + candidate ports),
  `FUNCTIONS.md`, `JUMP_MAP.md`, `PSEUDOCODE.md`, `disasm/NOTES.md`,
  and `TODO.md` to reflect the correction and the new findings; wired
  all 6 new/corrected names into `gen_disasm_x86.FUNCTIONAL_NAMES` and
  regenerated all listings/buildable `.asm`/readable+aligned artifacts
  (re-verified byte-identical / length-matching as before).

## Converted more raw-db fallbacks in the readable/aligned pipeline; found a genuine 8086 addressing quirk

- Fixed `xchg`'s reg/rm-swap ambiguity (`xchg di,si`/`xchg si,di` are
  the same operation, same opcode, swapped ModRM subfields - needed
  its own `alt_xchg_encoding()`, since the existing direction-bit-flip
  logic used for ALU ops/MOV doesn't apply to `xchg`'s opcode). Wired
  into all 4 places that already checked the other alternate-encoding
  classes (`validate_nasm.py`, `validate_2998.py`, `validate_all.py`,
  `gen_source_readable.py`).
- Confirmed and enabled `insb`/`insw`/`outsb`/`outsw` (previously
  excluded pending verification) - each assembles to the plain
  expected single byte with no unwanted prefix.
- Both fixes converted a handful of remaining raw-`db` fallback lines
  in the readable `.asm`/aligned `.bin` outputs into real instructions
  (with NOP padding where needed) - regenerated all 3 chips' aligned
  binaries.
- Investigated the 3 remaining not-independently-checked instructions
  (backward `loop`/`jmp` branches) by inspecting the exact `(seg,off)`
  pair each was reached with - found a genuine, well-documented 8086
  CPU behavior (relative-branch IP wraparound mod `0x10000` while CS
  stays fixed, landing outside the current chip's window because these
  specific segment values aren't 16-aligned), not a decode bug.
  **Not fully resolved**: still unclear whether the ROM intends this
  literally, or whether our recursive descent reaches this code via
  the "wrong" (though byte-valid) segment to begin with. See
  `disasm/NOTES.md` "IP-wraparound branches".
- Updated `disasm/NOTES.md` (validation status, coverage numbers
  refreshed to current: 13,510 proven-only instructions, 68,776 with
  the heuristic layer) and `TODO.md` to match; regenerated all
  listings, buildable `.asm`, and readable/aligned artifacts.

## Started a full renaming pass; found the readout vector display list

- User asked to keep renaming until everything is named. Started
  systematically working through the 270 unnamed proven-set functions
  (ordered by reference count) rather than stopping after one or two.
- First batch (21 functions) landed on a major architecture find:
  there are **two separate mechanisms** for printing to the readout,
  not one. Previously known: the hardware-port path
  (`print_string_far`/`print_char`/`write_readout_port_byte`, writing
  straight to physical `0x40000+0x6F0`). New this session: a
  **vector-stroke display-list path** - `print_readout_string` ->
  `draw_readout_char` -> `plot_readout_point_relative` ->
  `plot_readout_point`, which looks each character up in a stroke-font
  table at `[0x1DB0]` and appends `(y,x,attribute)` triples to a
  circular buffer at `[0x1CC4]`. This is the classic Tektronix
  "readout" character-generator format (vector strokes, not a
  bitmap) - independent corroboration this scope's CRT readout really
  is stroke-based. See `disasm/NOTES.md` "The readout vector display
  list".
- Also named the number-formatting engine behind this firmware's
  printf-style output: `format_number` (core radix/width/sign
  formatter) plus its `format_hex_word`/`format_decimal_word`/
  `format_word_radix`/`format_byte_hex` wrappers, and the bounded
  string utilities `strncat_far`/`strncpy_far`, plus `set_ds_return_old`
  (a segment-swap primitive called 40x in the comm ROM).
- **Corrected a real error**: `print_selftest_banner`'s pseudocode had
  wrongly assumed one of its calls (`SUB_E374E`, now
  `close_print_record`) printed a string - it doesn't traverse its
  far-pointer argument at all, it only tags a record's completion
  byte. Fixed in `PSEUDOCODE.md`.
- Regenerated all listings, buildable `.asm`, and readable/aligned
  artifacts after each addition - all still verify clean (byte-exact/
  length-matching, 0 real mismatches).
- Continued the renaming pass: `print_selftest_report_line`,
  `wait_readout_tick`, `clear_selftest_status_flags`, `seg_off_to_linear`,
  and identified `selftest_measure_and_report`/`selftest_measure_mode`
  as one of `self_test_dispatcher`'s 14 test calls (enable/run/disable
  shape confirmed; specific peripheral not yet confirmed).
- First comm-ROM (160-2998) renames: `set_ds_return_old` (a DS-segment-
  swap primitive, 40 call sites), `serial_tx_buffer_put` (a ring-buffer
  writer, likely GPIB/RS-232 TX), `set_comm_critical_flag` (a critical-
  section flag with a cross-ROM callback on unlock).
- Found a plausible (not fully confirmed) waveform-acquisition-buffer
  init block in `160-3532` (8 buffer-size variables all set to 0x800 =
  2048 bytes) and named its 2 entry stubs `reset_acq_buffers_stub`/
  `print_and_reset_acq_buffers`, plus a neighboring scale/divide
  utility pair `divide_scale`/`divide_scale_default`. See
  `disasm/NOTES.md` "Possible waveform acquisition buffer init".

## Full renaming pass, part 2: self-test subroutines identified, a validator bug fixed

User: "keep going... dont stop until 100%". Continued the systematic
renaming pass; 89/278 proven-set functions now named (up from 56 at
the start of this entry).

- **Landmark find**: identified essentially all ~19 of
  `self_test_dispatcher`'s sibling test subroutines in one sweep, by
  finding each one's `0xFF7B` string-table reference and reading the
  actual diagnostic label (`HS_ACQ`/`MM_ACQ`/`XY_ACQ`, `ACQ_RAM`,
  `ROMS`/`MISMATCH`, `COMM_ROM`/`COMM_RAM`/`CMOS`/`COMM_LB`, `CDT`,
  display-controller interrupt messages). This is a far more reliable
  method than the call-order guessing tried in earlier sessions (which
  found nothing) - now documented as a reusable technique in
  `CLAUDE.md` and automated in `disasm/scan_string_refs.py`.
- Found the missing link for how self-test results become printed
  text: `format_selftest_result_string`, confirming the bit encoding
  (`0x20`=not installed, `0x02`=failed, `0x01`=passed, else untested).
- Identified the ADC self-test's peripheral (front-panel A/D
  converter, `selftest_front_panel_adc`) and a generic RAM pattern-test
  engine (`ram_pattern_test`).
- Found the printf-style variadic formatting engine itself
  (`format_string_va`) and a parallel record-builder family in
  `160-3532` mirroring the `160-3633` print-record system.
- **Corrected a real error twice this session**: a full re-read of
  `self_test_dispatcher` found it had actually missed a chunk of the
  function's body, wrongly attributing several real test calls to the
  surrounding caller. And a much bigger one: the "3 backward branches
  exhibit a genuine 8086 IP-wraparound quirk" conclusion from the
  previous session was **wrong** - it was a masking bug in
  `validate_nasm.py`'s own target-computation code. Fixed by matching
  `gen_disasm_x86.py`'s approach (don't mask the raw offset before
  adding to the segment base). Net effect: only 1 raw-`db` instruction
  (the x87 `fmul`) remains anywhere in the readable/aligned pipeline
  across all three ROMs.
- Regenerated and re-verified everything (byte-exact/length-matching,
  0 real mismatches) after every batch, committing incrementally.

## Full renaming pass, part 3: task scheduler completed, HPGL plotter driver, halt_cpu/assert mechanism, comm ROM RS-232/GPIB clusters

Continued the same "keep going" renaming pass through the rest of the
day (89 → 190/278 proven-set functions named by the end of this
entry). Reconstructed from git history - not written up in real time
the way earlier sections were.

- **Closed out the task scheduler cluster**: `create_task` (spawns a
  fresh task - stack, saved SP/SS, entry point, ready flag - then
  jumps into `switch_to_next_task`; called from all three ROM address
  spaces, confirming it's a shared kernel primitive) plus its
  companions `restart_current_task`, `mark_task_ready`, `create_task_
  b` (a near-duplicate called specifically from `mark_task_ready`),
  `scheduler_tick_service` (the per-tick heartbeat inside `INT2_
  HANDLER_LATE`, reading hardware status bytes at physical
  `0x403FFA`/`0x403FFB` into `[0x758]`/`[0x759]` and incrementing
  `[0x752]` - the exact counter `wait_readout_tick` busy-waits on),
  and `scan_low_ram_chunk0`/`_1`/`_2` (a genuine background low-memory
  integrity watchdog, NOT a timing delay as first guessed for the
  similarly-shaped `delay_read_128w` - each covers a different 32-96
  byte slice, together spanning the first 256 bytes of the IVT across
  3 ticks).
- **Found the HPGL plotter output driver**: `update_plot_position`/
  `plot_line_to` emit HPGL `PU%d,%d;`/pen-down commands via `format_
  string_va`; neighboring code emits `SP1;SC0,1023,0,1023;` and
  HP-GL2 escape sequences right around I/O port `0x83` - a strong,
  still-unconfirmed candidate for that port being the GPIB/plotter
  output. `start_plot_output_task` sends a standard `ESC@` device-
  reset sequence before spawning the background plot job via
  `create_task`.
- **Found the top-level self-test orchestrator**: `run_selftest_
  sequence` ties `print_selftest_banner`, `self_test_dispatcher`,
  `init_selftest_report_screen`, and the readout display-list buffer
  into the actual entry point that starts a self-test run - found by
  tracing `print_selftest_report_line`'s callers, an open thread since
  it was first identified. Also `print_boot_rom_id_banner`, `verify_
  pattern_with_report`, and `verify_timebase_prc` (references sweep-
  speed divider names, suggesting `PRC` is a timebase/reference-clock
  counter - foreshadowing the next session's full `PRC_READBACK`
  mapping).
- **Resolved mystery I/O ports `0xD1`/`0xC4`**: `write_hw_shift_
  register` writes a serial shift-register-style pattern to both,
  reached as the default case of a table-driven front-panel-setting
  dispatch. **Documented the first decode anomaly** in the same
  commit: `SUB_EAC86` decodes as unambiguous garbage (including an
  impossible SSE instruction) despite being a clean far-call target
  reached identically from 3 places across 2 ROMs - left unrenamed
  rather than guessed, the start of a thread that grew substantially
  over the following two sessions.
- **Found the firmware's real `assert()`/panic mechanism**: `halt_cpu`
  (a bare `hlt`) is called from `assert_and_halt` with pushed
  diagnostic arguments that are never actually used (the `hlt` never
  returns, making the caller's own stack cleanup dead code) - a
  genuine abort trap, not a normal function. `assert_and_halt`
  computes its value via `convert_sample_value` (`0xF1001`), which
  opens with a real x87 `fmul` in the middle of otherwise ordinary
  compiled-C integer code - stronger evidence toward the open "is
  there a real 8087 coprocessor" question.
- **Corrected a real naming error**: `divide_scale`/`divide_scale_
  default` (named in an earlier session) were wrong - the actual math
  is multiply-by-reciprocal-then-shift (`mul32`+`ashr32`), not divide.
  Renamed to `scale_and_plot_point`/`scale_and_plot_point_default`,
  which reframed the earlier "possible waveform acquisition buffer
  init" finding as a plot-position cache reset instead. Found the real
  signed divide (`sdiv32`) and `plot_scaled_point` while investigating.
- **Corrected a real direction mistake**: the ring buffer at `[0x448]`/
  `[0x44C]` is a **TX** queue, not RX as assumed earlier this session -
  `service_comm_rx_queue`/`init_comm_rx_queue_and_ready_flags` renamed
  to their `_tx_` equivalents; no genuine incoming-data path identified
  yet (new `TODO.md` item).
- **Mapped a large comm-ROM RS-232/GPIB cluster**: RS-232 XON/XOFF
  flow control (`get_xon_xoff_byte`, `enqueue_comm_char` with space/
  mark parity), the reference-counted flow-control hold pair (`engage_
  comm_hold`/`release_comm_hold` and their `_critical` wrappers - which
  also confirmed `set_comm_critical_flag`'s real semantics as a mutex-
  like busy flag that wakes task 5 via `mark_task_ready`), the GPIB
  query-response printing cluster (`print_param_list_response`/`get_
  next_param_value`), `process_gpib_command_byte` (handles a received
  GPIB byte under the critical-section lock), `handle_gpib_device_
  clear`, the boot-time cross-ROM handoff (`comm_rom_boot_init` and its
  init helpers, gated on `check_comm_option_installed`'s result), and
  the comm option board's DIP-switch readers (`read_dip_switches_
  serial_config`/`read_dip_switches_gpib_config`, decoding baud rate,
  parity, CR/LF, GPIB address, and mode flags from 2 hardware switch
  bytes).
- **Found the source of `[0x1B83]`**: `detect_comm_option_hw`, a
  second, independent hardware probe for the comm option board
  (separate from `check_comm_option_installed`'s ROM-header-checksum
  check) - a write-then-readback test at physical `0x40000+0x7DE`/
  `0x377E`. Which specific readback value means "installed" wasn't
  resolved (`0x1E` results from two branches with opposite-looking
  conditions) - left open rather than guessed, and stayed open for two
  more sessions.
- **Added the first hardware reference photos** (`HARDWARE.md`): the
  comm option board's rear panel (dual RS-232 DTE/DCE connectors, a
  10-position PARAMETERS DIP switch, a 9-pin AUX connector with a
  pen-lift relay + analog X/Y outputs) and the front panel control
  layout - direct physical confirmation of the RS-232 flow-control
  cluster and the HPGL plot pipeline. Followed later the same day by
  71 photos walking the entire `ACQ_MODE_SETUP_TABLE` menu tree
  screen-by-screen plus a main-board interior photo (CPU, ROMs, Sony
  A/D chip) - confirming `SYS_ROM_0`/`SYS_ROM_1`/`COMM_ROM`/`ACQ_RAM`,
  `HS_ACQ`/`MAX_MIN_ACQ`/`XY_ACQ`/`FP_AtoD`/`COMM_READBACK`/`COMM_
  LOOPBACK` as real self-test dispatcher names, and the `SAMPLE`/
  `ACCPEAK`/`AVERAGE`/`PEAKDET` matrix matching `handle_acq_mode_
  change`'s bit dispatch exactly. New `hardware/convert_heic.py`
  script for the photo pipeline (HEIC→JPG, 25% scale/~85% quality).
- **User correction applied**: the two RS-232 connectors are just
  DTE/DCE pinouts of the same port for cabling convenience, not a
  firmware-visible mode select - removed the speculation that
  `[0x629]` might be a DTE/DCE switch from `HARDWARE.md`/`TODO.md`.
- **Documented a second decode anomaly**: `SUB_F6382` opens with a
  capstone-decoded impossible `movd`/MMX instruction - found evidence
  this is capstone misreading the undocumented 8086 1-byte opcode
  `0x0F` (`POP CS`) as a 286+ SSE/MMX escape prefix. Left unrenamed.
  Also **resolved an older open thread**: `SUB_E90A5`/`SUB_E92B0` are
  genuine `lcall` targets landing exactly 1 byte into a `mov di, tag`
  instruction belonging to a separate, already-understood fallthrough
  path through `assert_and_halt`'s repeating tag-dispatch pattern -
  not independent functions, just landing artifacts. The first of what
  became a much larger family of these across the next two sessions.
- Also renamed, among many smaller finds: `reset_all_channel_plot_
  caches` (8 separate plot-position caches, not just one pair),
  `compute_range_checksum`, `draw_box_outline`, `update_plot_retry_
  counters`, `write_measurement_channel_reg`, `refresh_report_display`,
  `refresh_display_position_cache`, `reinit_system_state`, `notify_
  comm_rom_a`/`b`, `set_item_active_flag`/`set_item_pair_active`/
  `deselect_item_pair`/`disable_item_pair`, `build_gpib_message_
  checksum`/`init_gpib_record`, `print_signed_decimal_serial`/`print_
  unsigned_decimal_serial`, `init_far_pointer_table`, `init_comm_
  device_table`, `handle_acq_mode_change` (plus its `reset_display_
  and_notify_comm` cluster), `spawn_task_with_tag`, `send_response_
  terminator`/`finish_comm_response`.
- **Open puzzle first noticed this session**: `compute_parity_mode_
  code` (comm ROM) brackets an unrelated DIP-switch dispatch with 2
  calls to `scale_and_plot_point_default` using the exact argument/
  save/restore shape of the `set_ds_return_old` DS-switch helper, but
  the confirmed call target is the real plot-scaling function - not
  resolved whether this is deliberate reuse or something else. A
  second instance (`plot_point_with_flag_update`, main ROM) turned up
  later the same session; the `SUB_F6382`-style "landing 1-byte-early"
  explanation was checked and ruled out for it specifically (no second
  decode path exists to disagree with).

190/278 proven-set functions named by the end of the day.
